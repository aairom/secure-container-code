apiVersion: v1
kind: ConfigMap
metadata:
  name: cpp-app-config
  namespace: secure-apps
data:
  PORT: "8080"
---
apiVersion: v1
kind: Secret
metadata:
  name: cpp-app-secrets
  namespace: secure-apps
type: Opaque
stringData:
  LICENSE_KEY: "DEMO-LICENSE-KEY"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secure-cpp-app
  namespace: secure-apps
  labels:
    app: secure-cpp-app
    language: cpp
    security: high
spec:
  replicas: 3
  selector:
    matchLabels:
      app: secure-cpp-app
  template:
    metadata:
      labels:
        app: secure-cpp-app
        language: cpp
        security: high
    spec:
      # Security context for the pod
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      
      containers:
      - name: cpp-app
        image: secure-cpp-app:latest
        imagePullPolicy: IfNotPresent
        
        # Container security context
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
              - ALL
        
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: cpp-app-config
              key: PORT
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: cpp-app-secrets
              key: LICENSE_KEY
        
        # Resource limits (C++ needs more memory than Go)
        resources:
          requests:
            memory: "32Mi"
            cpu: "20m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        
        # Health checks
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 2
        
        startupProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 0
          periodSeconds: 2
          timeoutSeconds: 2
          failureThreshold: 30
        
        # Volume mounts
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      
      volumes:
      - name: tmp
        emptyDir: {}
      
      # Pod anti-affinity
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - secure-cpp-app
              topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: secure-cpp-app
  namespace: secure-apps
  labels:
    app: secure-cpp-app
spec:
  type: ClusterIP
  selector:
    app: secure-cpp-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: secure-cpp-app-hpa
  namespace: secure-apps
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: secure-cpp-app
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

# Made with Bob
